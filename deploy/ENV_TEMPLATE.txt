# /etc/devbridge/env  (chmod 600, owner root:root)
# systemd EnvironmentFile= 형식 → KEY=VALUE 한 줄씩, 따옴표/공백 주의
DB_HOST=localhost
DB_PORT=3306
DB_NAME=devbridge_db
DB_USERNAME=devbridge
DB_PASSWORD=changeme

# 64자 이상 랜덤 문자열 권장. 기존 키가 있으면 그대로 사용 (세션 유지)
JWT_SECRET=__REPLACE_ME__

# 외부 서비스 키 (기존 값 그대로 옮겨오기)
GEMINI_API_KEY=__REPLACE_ME__
STREAM_CHAT_API_KEY=__REPLACE_ME__
STREAM_CHAT_API_SECRET=__REPLACE_ME__
MAIL_USERNAME=__REPLACE_ME__
MAIL_PASSWORD=__REPLACE_ME__

# 새 인스턴스 IP로 교체 (콤마 구분, http://IP 또는 http://도메인)
CORS_ALLOWED_ORIGINS=http://NEW_PUBLIC_IP

UPLOAD_DIR=/var/devbridge/uploads

# ───── 암호화 · 사이드카 (운영 필수) ─────
# KIS/Binance appsecret·사용자 Claude 키(BYOK) AES-GCM 암호화 키. Base64 32바이트. 미설정 시 부팅 실패(prod).
#   생성 예: openssl rand -base64 32
APP_CRYPTO_KEY=__REPLACE_ME__
# BE → Analytics(:8001) 사이드카 내부 인증 토큰 (analytics .env 의 값과 동일해야 함)
ANALYTICS_INTERNAL_TOKEN=__REPLACE_ME__

# ───── Developer Studio · Claude(BYOK) · 멀티세션 ─────
# Claude Code 에이전트 활성화. true 로 켜려면 서버에 claude CLI 설치 필수:
#   sudo npm i -g @anthropic-ai/claude-code   (Node 20+)
# 사용자별 BYOK 키(ANTHROPIC API)는 DB 에 AES-GCM 암호화 저장되고 요청 시 자식 프로세스 env 로만 주입된다.
# (서버 공용 ANTHROPIC_API_KEY 는 두지 않는다 = 배포물에 키 없음. 사용자 본인 키로만 동작.)
CLAUDE_CLI_ENABLED=true
# 항상 Developer Studio 접근 허용할 이메일(콤마 구분). 그 외에는 STANDARD/PREMIUM 구독자만 접근.
DEVELOPER_ALLOWLIST=admin@example.com,dev@example.com
# (선택) 서버 공용 Claude 키를 굳이 쓰려면 설정 — 권장하지 않음(배포물에 키가 남음). 비우면 BYOK 전용.
# ANTHROPIC_API_KEY=

# ───── 결제 · 브로커 (기존 운영값 그대로) ─────
TOSS_SECRET_KEY=__REPLACE_ME__
APPROVAL_HMAC_SECRET=__REPLACE_ME__
TRADING_KILL_SWITCH=false
